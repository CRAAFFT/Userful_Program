Buat Virtual Environment dengan command berikut

python -m venv .venv 
atau
python3 -m venv .venv
atau py -3 -m venv .venv

setelah itu jalankan 
.venv\Scripts\activate

Lalu jalankan 
pip install -r requirements.txt